import React from 'react';
import {Button} from '@material-ui/core';

const NewAbilityBtn = () => {
  return (
    <Button variant="contained" color="primary">
      New Ability
    </Button>
  );
};

export default NewAbilityBtn
